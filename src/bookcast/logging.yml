version: 1
formatters:
  console:
    format: "%(levelname)s [%(asctime)s] %(name)s - %(message)s"
    datefmt: "%Y-%m-%d %H:%M:%S"
handlers:
  console:
    class: logging.StreamHandler
    formatter: console
    stream: ext://sys.stdout
root:
  level: INFO
  handlers: [console]
